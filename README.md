# 企鹅健身房

一个无构建依赖的浏览器合成消消乐原型。玩法参考合成经营循环：从企鹅背包补给鱼干/虾干、拖动或点选相同食材合成、完成企鹅会员订单、赚金币升级。

## 本地运行

在本目录启动静态服务器：

```powershell
python -m http.server 5173
```

然后访问：

```text
http://localhost:5173
```

当前 Codex 环境已经带有可用的 Python/Node 运行时，因此这个原型不需要额外下载依赖。后续如果要接入 React/Vite、资源打包或移动端封装，再安装 Node.js 和 npm 会更合适。
